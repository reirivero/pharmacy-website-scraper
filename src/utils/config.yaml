# config.yaml
paths:
  input_file: './data/input_urls.csv'
  output_file: './data/output_data.csv'
  log_file: './logs/extract_data.log'

logging:
  level: 'ERROR'
  format: '%(asctime)s:%(levelname)s:%(message)s'